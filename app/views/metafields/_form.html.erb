<%= form_with(model: metafield, local: true) do |form| %>
    <% if metafield.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(metafield.errors.count, "error") %> prohibited this metafield from being saved:</h2>
  
        <ul>
        <% metafield.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :key %>
      <%= form.text_field :key %>
    </div>
  
    <div class="field">
      <%= form.label :namespace %>
      <%= form.text_field :namespace %>
    </div>
  
    <div class="field">
      <%= form.label :value %>
      <%= form.text_area :value %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  